spring:
  application:
    name: aib-backend

  profiles:
    active: dev

  jpa:
    properties:
      hibernate:
        procedure:
          auto_named_procedures: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: mohamedmkacher06@gmail.com
    password: xirakvkkaixlkcrj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    default-encoding: UTF-8
server:
  port: 8088
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    com.aib.backend: DEBUG
    org.springframework.security: DEBUG